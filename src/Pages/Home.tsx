import React from "react";
import { DataGrid, Navbar } from "../Components";

const Home: React.FC = () => {
  return (
    <main>
      <Navbar />
      <DataGrid />
    </main>
  );
};

export default Home;
